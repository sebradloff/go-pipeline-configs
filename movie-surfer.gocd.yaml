pipelines:
  MovieSurferPipe:
    group: MovieSurfer
    materials:
      mygit:
        git: https://github.com/sebradloff/movie-surfer.git
    stages:
      - build:
          jobs:
            build:
              tasks:
                script: echo "HELLO"
